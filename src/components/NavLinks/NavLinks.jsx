// Classes from index.css

export default function NavLinks({ className }) {
  return (
    <nav className={className}>
      <a href="#">Shop</a>
      <a href="#">About us</a>
      <a href="#">Contacts</a>
    </nav>
  );
}
